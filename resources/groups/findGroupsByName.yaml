get:
  summary: لیست گروه ها
  operationId: findGroupsByName
  description: لیست گروه ها
  tags:
    - commonServices
  requestBody:
    description: اطلاعات جستجو
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/common/findGroupsByNameRequestBody.yaml'
  responses:
    '200':
      description: Successful operation
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/common/findGroupsByNameResponse.yaml'
    '400':
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/exceptions/validation-error.yaml'
    '500':
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/exceptions/error.yaml'
    '204':
      description: There is no response
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/exceptions/entityNotFoundException.yaml'